'use strict';

/**
 * record-organization controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::record-organization.record-organization');
