'use strict';

/**
 * record-interview controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::record-interview.record-interview');
