'use strict';

/**
 * course-fit-for controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::course-fit-for.course-fit-for');
