'use strict';

/**
 * record-organization router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::record-organization.record-organization');
