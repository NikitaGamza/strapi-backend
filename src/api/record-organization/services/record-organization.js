'use strict';

/**
 * record-organization service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::record-organization.record-organization');
